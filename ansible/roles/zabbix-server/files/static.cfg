server {
        listen 80 default_server;
        listen [::]:80 default_server;
        zabbix /usr/share/zabbix;
        server_name _;
        location / {
                try_files $uri $uri/ =404;
        }
}
